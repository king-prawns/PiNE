enum PlayerState {
  UNKNOWN = 'UNKNOWN',
  LOADING = 'LOADING',
  BUFFERING = 'BUFFERING',
  PLAYING = 'PLAYING',
  PAUSED = 'PAUSED',
  ENDED = 'ENDED',
  ERRORED = 'ERRORED'
}

export default PlayerState;
